use ggez::graphics::Canvas;
use ggez::{Context, GameResult};

pub fn draw_pause_screen(_context: &mut Context, _canvas: &mut Canvas) -> GameResult {
    // Placeholder stub for pause screen UI
    Ok(())
}
